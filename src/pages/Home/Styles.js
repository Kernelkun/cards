import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
  container: {
    paddingTop: '10em'
  },
  video: {
    position: 'relative',
    height: '100vh',
    width: '100%',
    minHeight: '700px'
  }
}))
